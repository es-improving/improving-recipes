No unit tests for this feature.

All acceptance criteria are purely visual (text color rendered in the browser). The implementation is a CSS-only change with no modifications to component logic, API, or data layer.
